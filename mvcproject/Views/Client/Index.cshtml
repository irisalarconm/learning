@model IEnumerable<mvcproject.Models.Client>

@{
    ViewData["Title"] = "Index";
}

<h1>Clients</h1>
<h2>Index</h2>

<p>
    <a asp-action="Create">Create new client</a>
</p>
<table class="table">
    <thead>
        <tr> <!--fila-->
            <th>@Html.DisplayNameFor(model=>model.NameClient)</th>
            <th>@Html.DisplayNameFor(model=>model.LastnameClient)</th>
            <th>@Html.DisplayNameFor(model=>model.DniClient)</th>
            <th>@Html.DisplayNameFor(model=>model.AddressClient)</th>
            <th>@Html.DisplayNameFor(model=>model.Phone)</th>
            <th>@Html.DisplayNameFor(model=>model.Status)</th>
            <th>Actions</th>
        </tr>
    </thead>
    @* <tbody>
@foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem=>item.NameClient)</td>
                <td>@Html.DisplayFor(modelItem=>item.LastnameClient)</td>
                <td>@Html.DisplayFor(modelItem=>item.DniClient)</td>
                <td>@Html.DisplayFor(modelItem=>item.AddressClient)</td>
                <td>@Html.DisplayFor(modelItem=>item.Phone)</td>
                <td>@Html.DisplayFor(modelItem=>item.Status)</td>
                <td>
                    <button asp-action="Edit" class="btn btn-warning" asp-route-id="@item.ClientId">Edit</button>
                    <button asp-action="Details" class="btn btn-warning" asp-route-id="@item.ClientId">Details</button>
                    <button asp-action="Delete" class="btn btn-warning" asp-route-id="@item.ClientId">Delete</button>
                </td>
            </tr>

        }

    </tbody> *@
</table>